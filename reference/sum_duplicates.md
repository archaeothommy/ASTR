# Sum columns with same column name

The function recognises and includes column names that in fact have the
same name but were made unique by R functions.

## Usage

``` r
sum_duplicates(df)
```

## Arguments

- df:

  A data frame
